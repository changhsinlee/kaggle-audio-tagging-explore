from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.0.1',
    description='Chang's personal repo for Kaggle audio tagging code',
    author='SummitML',
    license='MIT',
)
